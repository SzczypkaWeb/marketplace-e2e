{
  "name": "marketplace-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "node test-readme-ci-docs.js",
    "test": "playwright test",
    "test:marketing": "playwright test --project=marketing",
    "test:app": "playwright test --project=app",
    "test:flows": "playwright test --project=flows",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install-browsers": "playwright install --with-deps",
    "test:docs": "node test-readme-ci-orchestration.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "argon2": "^0.45.1",
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  }
}
