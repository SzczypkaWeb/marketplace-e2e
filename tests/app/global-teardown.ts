// DEPRECATED — replaced by auth.teardown.ts (Playwright "teardown project",
// see playwright.config.ts). No longer referenced by playwright.config.ts's
// `globalTeardown` (removed - see the note in global-setup.ts for why). Could
// not delete this file directly (sandbox filesystem denied the unlink) -
// please `git rm tests/app/global-teardown.ts` manually.
export {};
